spring:
  cloud:
    config:
      name: application-pay
      label: yml
      discovery:
        service-id: CONFIG-SERVER
        enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1000/eureka/
#      profile: