spring:
  cloud:
    config:
      name: application-zuul
      label: master
      discovery:
        service-id: config-server
        enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1000/eureka/
#      profile: