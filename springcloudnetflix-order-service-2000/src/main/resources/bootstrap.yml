spring:
  cloud:
    config:
      name: application-order
      label: ymltest
      discovery:
        service-id: CONFIG-SERVER
        enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1000/eureka/
#      profile: